import contract from "./cypherskulls.json";

export const ADDRESS = ""; // pass in the contract address, here

// Application Binary Interface - allow frontend to interact with smart contract
export const { abi: ABI } = contract;
